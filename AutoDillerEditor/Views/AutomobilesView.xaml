<UserControl x:Class="AutoLandProcessor.Views.AutomobilesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:AutoLandProcessor.Views" xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
			 mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition/>
		</Grid.RowDefinitions>

		<DockPanel Grid.Row="0" Margin="10">
			<StackPanel Orientation="Horizontal"
		DockPanel.Dock="Left">

				<md:PackIcon Kind="CarMultiple"
			 HorizontalAlignment="Center"
			 VerticalAlignment="Center"
			 Width="32" Height="32" Margin="5"/>

				<TextBlock Text="РАБОТА С АВТОМОБИЛЯМИ"
		   VerticalAlignment="Center"
		   Style="{StaticResource MaterialDesignHeadline6TextBlock}"/>
			</StackPanel>

			<StackPanel  HorizontalAlignment="Right"
		 Orientation="Horizontal"
		 DockPanel.Dock="Right">

				<Button Content="Load" Margin="5" Command="{Binding LoadCommand}"/>
				<Button Content="Save" Margin="5"/>
				<Button Content="Add"  Margin="5"/>

			</StackPanel>
		</DockPanel>

		<DataGrid Grid.Row="1"
				  ItemsSource="{Binding Items}" AutoGenerateColumns="True">

		</DataGrid>
	</Grid>
</UserControl>
