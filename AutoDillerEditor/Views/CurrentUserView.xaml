<UserControl x:Class="AutoLandProcessor.Views.CurrentUserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:AutoLandProcessor.Views"
             mc:Ignorable="d"
             d:DesignHeight="80" d:DesignWidth="300">

    <Grid Margin=" 5">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto"/>
			<ColumnDefinition Width="*"/>
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<Image Grid.Row="0"
			   Grid.RowSpan="3"
			   Grid.Column="0"
			   Margin="10"
			   Source ="{Binding Avatar}" Width="32" Height="32"/>
		<TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding FullName}" FontSize="18"/>
		<TextBlock Grid.Row="1" Grid.Column="1" Margin="0" Text="{Binding Phone}" FontSize="12"/>
		<TextBlock Grid.Row="2" Grid.Column="1" Margin="0" Text="{Binding Email}" FontSize="12"/>
	</Grid>
</UserControl>
