<UserControl x:Class="AutoLandProcessor.Views.MainContentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:views="clr-namespace:AutoLandProcessor.Views"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

	<UserControl.Resources>
		<BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

		<DataTemplate x:Key="NavigationViewHeaderTemplate">
			<TextBlock Text="{Binding}" Margin="12,32,0,0" />
		</DataTemplate>

	</UserControl.Resources>

	<Grid>
		<TabControl>
			<TabItem Header="USERS">
				<views:UsersView DataContext="{Binding UsersViewModel}"/>
			</TabItem>

			<TabItem Header="AUTOMOBILES">
				<views:AutomobilesView DataContext="{Binding AutomobilesViewModel}"/>
			</TabItem>

			<TabItem Header="DEALERS">
				<views:DealersView DataContext="{Binding DealersViewModel}"/>
			</TabItem>

			<TabItem Header="CONTRACTS">
				<views:ContractsView DataContext="{Binding ContractsViewModel}"/>
			</TabItem>

		</TabControl>
	</Grid>

</UserControl>
